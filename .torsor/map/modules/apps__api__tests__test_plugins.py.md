---
type: map
status: derived
tags:
- map
links: []
created: '2026-09-17T04:36:24'
updated: '2026-09-17T04:36:24'
---

# apps/api/tests/test_plugins.py

Symbols in `apps/api/tests/test_plugins.py`.

- L44 `_resolve_the_example_host(monkeypatch: pytest.MonkeyPatch)` (function) — `apps.example.com` has no DNS record, and the guard refuses names that do not
- L61 `team(client: Client)` (function)
- L68 `install(owner: Client, **overrides: object)` (function)
- L75 `bot_client(owner: Client, token: str)` (function) — A caller with a bot token and no session cookie — how an app really connects.
- L83 `test_every_event_maps_to_a_scope_that_exists()` (function)
- L89 `test_no_presence_or_typing_event_is_offered()` (function)
- L96 `test_subscribing_needs_the_matching_scope()` (function)
- L103 `test_unknown_scopes_and_events_are_refused()` (function)
- L113 `test_a_signature_verifies()` (function)
- L119 `test_a_signature_does_not_survive_a_changed_body()` (function)
- L125 `test_a_captured_request_stops_working()` (function)
- L135 `test_the_wrong_secret_does_not_verify()` (function)
- L141 `test_missing_or_unparseable_headers_are_refused(timestamp: str | None, signature: str | None)` (function)
- L148 `test_backoff_grows_and_then_gives_up()` (function)
- L155 `test_jitter_stays_inside_its_bound()` (function)
- L162 `test_installing_returns_secrets_exactly_once(team: dict)` (function)
- L178 `test_a_member_cannot_install_or_list_apps(team: dict)` (function)
- L183 `test_the_bot_is_a_real_user(team: dict)` (function)
- L207 `test_a_second_app_cannot_take_the_same_slug(team: dict)` (function)
- L214 `test_a_bot_named_after_a_person_still_installs(team: dict)` (function)
- L223 `test_installing_is_audited(team: dict)` (function)
- L238 `test_a_malformed_request_url_is_refused_as_input(team: dict, url: str)` (function)
- L254 `test_a_private_address_is_a_policy_refusal_not_a_typo(team: dict, url: str)` (function)
- L263 `test_a_local_plugin_cannot_be_installed_over_http(team: dict)` (function)
- L273 `test_auth_test_reports_what_the_token_can_do(team: dict)` (function)
- L283 `test_a_bad_token_gets_nowhere(team: dict, token: str)` (function)
- L291 `test_a_session_cookie_is_not_an_app_token(team: dict)` (function)
- L297 `test_an_app_must_join_a_channel_before_posting(team: dict)` (function)
- L314 `test_an_app_cannot_reach_a_private_channel_it_was_not_invited_to(team: dict)` (function)
- L331 `test_a_missing_scope_is_refused_with_a_useful_code(team: dict)` (function)
- L345 `test_an_app_retrying_a_post_does_not_double_it(team: dict)` (function)
- L358 `test_an_app_cannot_edit_someone_elses_message(team: dict)` (function)
- L371 `test_a_disabled_app_can_do_nothing(team: dict)` (function)
- L384 `test_revoking_tokens_locks_an_app_out_immediately(team: dict)` (function)
- L394 `queued(plugin_id: str)` (function)
- L405 `test_a_message_queues_a_delivery(team: dict)` (function)
- L411 `test_only_subscribed_events_are_queued(team: dict)` (function)
- L419 `test_a_disabled_app_is_not_queued_for(team: dict)` (function)
- L427 `test_a_rejected_send_queues_nothing(team: dict)` (function) — The outbox row and the message commit together, or neither does.
- L440 `test_an_app_is_not_woken_by_its_own_message(team: dict)` (function)
- L450 `test_two_apps_each_get_their_own_delivery(team: dict)` (function)
- L458 `test_deliveries_are_visible_to_an_admin(team: dict)` (function)
- L467 `test_a_pending_delivery_says_when_it_will_be_tried(team: dict)` (function)
- L476 `test_the_delivery_detail_returns_the_payload_the_app_was_sent(team: dict)` (function)
- L489 `test_a_delivery_belonging_to_another_app_is_not_readable(team: dict)` (function)
- L501 `test_a_member_cannot_read_a_delivery(team: dict)` (function)
- L511 `test_the_workspace_delivery_log_lists_every_app(team: dict)` (function)
- L524 `test_a_failed_delivery_can_be_replayed(team: dict)` (function)
- L545 `test_a_pending_delivery_cannot_be_replayed(team: dict)` (function)
- L558 `test_a_member_cannot_replay_a_delivery(team: dict)` (function)
- L571 `test_an_update_that_widens_scopes_waits_for_approval(team: dict)` (function)
- L594 `test_an_update_that_narrows_scopes_takes_effect_at_once(team: dict)` (function)
- L611 `test_the_consent_screen_lists_exactly_what_the_update_added(team: dict)` (function)
- L626 `test_asking_twice_does_not_launder_a_pending_scope(team: dict)` (function)
- L649 `test_declining_keeps_the_app_running_on_its_old_permissions(team: dict)` (function)
- L667 `test_declining_with_nothing_pending_is_refused(team: dict)` (function)
- L674 `test_an_update_that_withdraws_its_ask_unparks_the_app(team: dict)` (function)
- L689 `test_setting_a_budget_shows_on_the_app(team: dict)` (function)
- L707 `test_a_zero_budget_is_refused_as_input(team: dict)` (function)
- L716 `test_a_budget_cannot_be_set_across_workspaces(team: dict)` (function)
- L729 `test_a_member_cannot_set_a_budget(team: dict)` (function)
- L737 `test_a_slug_cannot_change_after_install(team: dict)` (function)
- L746 `test_rotating_the_secret_returns_a_different_one(team: dict)` (function)
- L754 `test_uninstalling_keeps_what_the_bot_said(team: dict)` (function)
- L784 `test_uninstalling_stops_the_token(team: dict)` (function)
- L791 `test_uninstalling_is_audited(team: dict)` (function)
- L799 `TestAnAppHearsOnlyWhatItCouldRead` (class) — The push side has to agree with the pull side.
- L809 `test_a_public_channel_is_heard_without_joining(self, team: dict)` (method)
- L815 `test_a_private_channel_is_not_heard(self, team: dict)` (method)
- L825 `test_a_private_channel_is_heard_once_the_bot_is_in_it(self, team: dict)` (method)
- L839 `test_a_direct_message_is_not_heard(self, team: dict)` (method)
- L851 `test_a_channel_event_without_a_channel_is_refused(team: dict)` (function) — The guard that keeps this fixed.
- L871 `TestAppChannels` (class) — An installed app is inert until its bot joins a channel.
- L880 `test_public_channels_are_listed_with_membership(self, team: dict)` (method)
- L888 `test_an_admin_can_put_the_bot_in_a_channel_and_take_it_out(self, team: dict)` (method)
- L905 `test_private_channels_are_not_listed(self, team: dict)` (method)
- L918 `test_a_member_cannot_move_an_app_around(self, team: dict)` (method)
- L927 `test_install_can_be_given_its_signing_secret(team: dict)` (function) — A seeded agent's secret comes from the environment, not from us.
- L962 `TestInstructionsAndTheEverywhereSwitch` (class) — The two controls a workspace admin has over the agent the workspace was given.
- L978 `seeded(owner: Client, **overrides: object)` (method) — A row shaped like the one the seeder plants: this slug, this runtime, nobody's.
- L994 `members_of(owner: Client, channel_id: str)` (method)
- L998 `found_public(owner: Client, name: str)` (method)
- L1003 `test_instructions_are_stored_and_come_back_on_the_row(self, team: dict)` (method)
- L1017 `test_a_new_app_starts_with_neither(self, team: dict)` (method)
- L1024 `test_surrounding_whitespace_is_not_stored(self, team: dict)` (method)
- L1034 `test_nothing_to_say_clears_them(self, team: dict, cleared: str | None)` (method)
- L1050 `test_a_body_with_no_text_at_all_is_not_a_clear(self, team: dict)` (method)
- L1068 `test_a_body_with_no_enabled_at_all_does_not_turn_the_switch_off(self, team: dict)` (method)
- L1081 `test_more_than_four_thousand_characters_is_refused(self, team: dict)` (method)
- L1092 `test_exactly_four_thousand_characters_is_allowed(self, team: dict)` (method)
- L1099 `test_the_cap_is_measured_after_the_trim(self, team: dict)` (method)
- L1112 `test_the_audit_row_records_the_length_and_not_the_text(self, team: dict)` (method)
- L1129 `test_a_persons_own_agent_is_not_the_workspaces_to_instruct(self, team: dict)` (method)
- L1148 `test_a_persons_own_agent_is_not_the_workspaces_to_place(self, team: dict)` (method)
- L1164 `test_an_app_installed_by_hand_takes_no_instructions(self, team: dict)` (method)
- L1180 `test_an_app_installed_by_hand_is_not_seated_everywhere(self, team: dict)` (method)
- L1198 `test_the_switch_puts_the_bot_in_a_channel_founded_afterwards(self, team: dict)` (method)
- L1216 `test_the_switch_changes_no_channel_that_already_exists(self, team: dict)` (method)
- L1230 `test_turning_the_switch_off_stops_the_joining(self, team: dict)` (method)
- L1244 `test_handing_the_agent_to_a_person_stops_the_joining(self, team: dict)` (method)
- L1267 `test_the_switch_is_audited(self, team: dict)` (method)
- L1278 `test_a_member_controls_neither(self, team: dict)` (method)
- L1294 `test_neither_route_reaches_another_workspaces_agent(self, team: dict)` (method)
