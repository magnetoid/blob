---
type: map
status: derived
tags:
- map
links: []
created: '2026-09-04T17:42:51'
updated: '2026-09-04T17:42:51'
---

# apps/api/tests/test_bad_ids.py

Symbols in `apps/api/tests/test_bad_ids.py`.

- L29 `team(client: Client)` (function)
- L42 `TestAMalformedId` (class)
- L43 `test_is_a_client_error_on_every_shape_of_route(self, team: dict)` (method)
- L58 `test_says_what_is_wrong_without_quoting_a_regex(self, team: dict)` (method)
- L66 `test_covers_the_pagination_cursors_too(self, team: dict)` (method)
- L77 `TestAWellFormedIdThatNamesNothing` (class)
- L78 `test_is_still_a_404(self, team: dict)` (method)
- L86 `test_is_indistinguishable_from_one_you_cannot_see(self, team: dict)` (method)
- L96 `TestARealIdStillWorks` (class)
- L97 `test_history_and_its_cursor(self, team: dict)` (method)
- L109 `TestAMalformedIdInsideABody` (class) — The same fault, arriving by a different door.
- L119 `test_a_reply_to_a_malformed_thread_root(self, team: dict)` (method)
- L131 `test_every_other_body_that_carries_one(self, team: dict)` (method)
- L146 `test_an_ordinary_send_is_untouched(self, team: dict)` (method)
